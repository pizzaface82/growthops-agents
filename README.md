# GrowthOps Quick-Start 🌱
Simple Streamlit app to validate Python + environment setup.

## Run locally
```bash
python -m venv venv
.\venv\Scripts\Activate.ps1     # or source venv/bin/activate (Mac)
pip install -r requirements.txt
python -m streamlit run app.py
```
